# class TradesController < ApplicationController



#   private
#   def user_params
#     params.require(:user).permit(:date, :time, :currency_id, :type_id, :entry, :exit, :holding_time_id, :pip, :fee, :profit_loss, :win_lose_id).merge(user_id: current_user.id)
#   end
# end
